# Go SDK

DeepHarness 平台共享 Go SDK，包含 DDD 领域模型、基础设施抽象和通用工具。

## 结构

- `domain/` — 领域模型定义（identity, project, workitem, agent, audit）
- `infrastructure/` — 基础设施抽象接口（git, workitem-tracker, pr-agent, llm）
- `common/` — 通用工具函数
