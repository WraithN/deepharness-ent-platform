apiVersion: v1
kind: Pod
metadata:
  name: agent-runtime
  namespace: deepharness
spec:
  containers:
    - name: agent-runtime
      image: deepharness/agent-runtime:latest
      ports:
        - containerPort: 8090
      resources:
        limits:
          memory: "2Gi"
          cpu: "1000m"
