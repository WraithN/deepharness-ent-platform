PORT=8091
# Fastify 服务器端请求超时（必须 > CRAWL_TIMEOUT_MS，确保 BFS 总体超时能先触发返回部分结果）。
REQUEST_TIMEOUT_MS=100000
# BFS 多页遍历的总体超时：超时后停止开新页，返回已抓取的部分结果。
CRAWL_TIMEOUT_MS=90000
# 单页 page.goto 的超时上限（BFS 中实际取 min(PAGE_TIMEOUT_MS, 剩余总体时间)）。
PAGE_TIMEOUT_MS=30000
BROWSER_HEADLESS=true
LOG_LEVEL=info
