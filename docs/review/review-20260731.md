# dh-backend 后端架构 Review 报告

> 审查日期：2026-07-31
> 审查范围：`apps/dh-backend` 全部 `.go` 文件（排除 `_test.go`）
> 审查维度：架构边界、接口设计、错误处理、并发安全、魔法值、代码重复、SQL 安全、路径安全、函数复杂度、依赖管理、配置管理、日志规范、资源管理、WebSocket/SSE 连接管理

---

## 一、严重问题（P0，需立即修复）

### 1.1 SSH 私钥明文存储并通过 API 泄露

| 项目 | 说明 |
|------|------|
| 文件 | `packages/go-sdk/domain/repository/repository.go:13` |
| 文件 | `domain/repository/handler.go:38,64,89,106` |
| 严重程度 | **[严重]** |
| 描述 | `Repository.SSHKey string` 字段以明文存入数据库 `repositories.ssh_key` 列（`db_service.go:134` INSERT），无加密。`handler.go` 中 `json.NewEncoder(w).Encode(repo)` 直接序列化完整 `Repository` 对象，**SSH 私钥随 GET/POST/PUT 响应返回给客户端**，handler 未做字段过滤。 |
| 修复建议 | 1. DB 存储时加密 SSH key（AES-GCM）<br>2. API 响应中使用独立 DTO，永远不返回 `sshKey` 字段<br>3. 日志中已确认不记录 key 本身（良好） |

### 1.2 全代码库 0 处 `recover()`，18 处 goroutine panic 将崩溃整个进程

| 项目 | 说明 |
|------|------|
| 严重程度 | **[严重]** |
| 描述 | 全代码库 `recover()` 调用数为 **0**。共 18 处 `go` 启动的 goroutine 均无 `defer recover()` 保护，任何 panic 将导致整个进程崩溃。 |
| 关键位置 | `orchestrator/orchestrator.go:139`（编排流程）<br>`domain/repository/service/db_service.go:141,180,219,535,1493,1577`（仓库同步，6处）<br>`gateway/handler/gatewayd_proxy.go:160,176`（WebSocket 代理）<br>`domain/feishu/handler.go:85`（飞书事件处理）<br>`domain/agentconfig/handler.go:176`（agent 配置同步）<br>`domain/notification/handler.go:151`（通知回调）<br>`domain/workitem/handler.go:136`（工作项回调）<br>`agent/chat/session/session.go:35`（session reaper）<br>`agent/client/http.go:70`（HTTP 客户端）<br>`agent/client/agui_client.go:333,568`（SSE 读取） |
| 修复建议 | 提取通用 `safeGo(fn func())` 工具函数，包装 `defer recover()` + 日志，替换所有 `go func(){...}()` 调用。 |

### 1.3 `requirementsSummaryCache` 全局缓存无锁保护，数据竞争

| 项目 | 说明 |
|------|------|
| 文件 | `gateway/handler/stats.go:337-341`（定义），`:359-361`（读），`:385-387`（写） |
| 严重程度 | **[严重]** |
| 描述 | `requirementsSummaryCache` 是包级全局可变 struct，`WorkItemSummary` handler 中**无任何 mutex 保护**直接读写。并发 HTTP 请求同时调用时会产生数据竞争，可能导致缓存损坏或 panic。 |
| 修复建议 | 添加 `sync.RWMutex` 保护读写，或改用 `sync.Map`。 |

### 1.4 `code_write_node` 忽略 session/message 持久化错误

| 项目 | 说明 |
|------|------|
| 文件 | `orchestrator/code_write_node.go:69,83-89,105-111` |
| 严重程度 | **[严重]** |
| 描述 | <br>1. `_ = deps.Sessions.Create(fc.Ctx, chat.Session{...})` 忽略 session 创建错误。若创建失败，后续仍以该 `sessionID` 追加消息和调用 gatewayd，导致数据不一致。<br>2. `deps.Messages.Append(fc.Ctx, sessionID, chat.Message{...})` 的返回值（error）完全未被接收或检查。用户消息和 AI 回复的持久化失败均被静默吞没，可能导致消息丢失且无任何日志。 |
| 修复建议 | session 创建失败应中止流程并返回错误；message 持久化失败应记录日志并决定是否重试。 |

### 1.5 飞书 token 数据竞争（3 处）

| 项目 | 说明 |
|------|------|
| 文件 | `domain/feishu/service/replier.go:168`（读 token 未持锁）vs `:133`（写 token 持锁） |
| 文件 | `domain/feishu/service/cardkit.go:325`（读未持锁）vs `:313`（写持锁） |
| 文件 | `domain/feishu/service/group_history.go:40-47`（struct **完全无 mutex 字段**） |
| 严重程度 | **[严重]** |
| 描述 | 三个结构体各自实现了 `ensureToken` 逻辑，但 token 读取时未持锁，并发调用时存在数据竞争。`GroupHistoryFetcher` 完全没有 mutex 字段。 |
| 修复建议 | 提取共享 `FeishuTokenManager` 组合复用，统一用 `sync.Mutex` 保护 token 读写，解决 3 处数据竞争。 |

### 1.6 `resolveWorkspacePath` 缺少路径遍历防护

| 项目 | 说明 |
|------|------|
| 文件 | `gateway/handler/workspace_path.go:33-43` |
| 严重程度 | **[严重]** |
| 描述 | `resolveWorkspacePath` 直接 `filepath.Join(workspaceRoot, userID, workspaceID)` 后返回，**未校验** `userID` 和 `workspaceID` 是否包含 `..` 或 `/` 等路径遍历字符。`userID = ".."` 会导致路径逃逸到 workspaceRoot 的上级目录。 |
| 对比 | `domain/productspace/service/db_service.go:240-248` 的 `validateID` 函数有完整校验。 |
| 修复建议 | 在 `resolveWorkspacePath` 中调用 `validateID` 校验 `userID` 和 `workspaceID`。 |

### 1.7 `AgentRun()` 上帝函数 - 766 行、圈复杂度 157

| 项目 | 说明 |
|------|------|
| 文件 | `gateway/handler/agui.go:192-957` |
| 严重程度 | **[严重]** |
| 描述 | 承担请求校验、session 复用、workspace 路径解析、斜杠指令拦截、意图识别、SSE 流式转发、消息落库、错误恢复等十余种职责。违反 AGENTS.md 规则4（最多 3 层嵌套）和规则6（重复逻辑封装）。 |
| 修复建议 | 按职责拆分为 `resolveSession()`、`interceptCommands()`、`streamAgentRun()`、`persistMessages()` 等子函数。 |

### 1.8 4 个胖接口违反接口隔离原则

| 接口 | 文件 | 方法数 | 混杂职责 | 严重程度 |
|------|------|--------|---------|----------|
| `ProductSpaceService` | `domain/productspace/service/service.go:32` | 29 | 产品树CRUD+版本+评论+分享+导入+文件服务+清理 | **[严重]** |
| `WorkspaceService` | `domain/workspace/service/service.go:36` | 24 | workspace CRUD+成员+agent+规范+CICD | **[严重]** |
| `ProductDocService` | `domain/productdoc/service/service.go:15` | 23 | 文档CRUD+版本+文件夹+物化+分享+评论 | **[严重]** |
| `TeamService` | `domain/team/service/service.go:192` | 22 | Skill+Prompt+分类+统计（应拆为 SkillService+PromptService） | **[严重]** |

---

## 二、中等问题（P1/P2）

### 2.1 `context.Background()` 滥用（90 处），操作无法被取消

| 项目 | 说明 |
|------|------|
| 严重程度 | **[中等]** |
| 描述 | 90 处 `context.Background()` 调用（排除测试），导致操作无法被调用方取消。 |
| 关键位置 | `gateway/handler/agui.go`（14处）- 客户端断开后 agent 交互继续运行<br>`domain/repository/service/db_service.go`（28处）- 所有 stubclient 调用均使用 `context.Background()`<br>`gateway/server/server.go:463,489,515,529` - 向 orchestrator 传递 `context.Background()`<br>`domain/productspace/service/db_service.go`（7处）- 所有 stub* helper 函数 |
| 修复建议 | 至少在 service 层接收并传递调用方 context，使操作可取消。 |

### 2.2 错误类型判断用字符串匹配，脆弱

| 项目 | 说明 |
|------|------|
| 严重程度 | **[中等]** |
| 描述 | 多处通过 `strings.Contains(err.Error(), ...)` 判断错误类型，依赖错误消息文本而非类型化错误。 |
| 关键位置 | `gateway/handler/common.go:57` - `strings.Contains(err.Error(), "not found")` 判断 404<br>`domain/workspace/prompt_handler.go:155,230,311` - 同上 + `"builtin category"`<br>`domain/feishu/service/dispatcher.go:401` - `"already exist"`<br>`domain/repository/service/db_service.go:1682` - `"nothing to commit"` |
| 修复建议 | 定义哨兵错误（`var ErrNotFound = errors.New(...)`) 或自定义错误类型，用 `errors.Is`/`errors.As` 判断。 |

### 2.3 工作区路径构建重复 11+ 处

| 项目 | 说明 |
|------|------|
| 严重程度 | **[中等]** |
| 描述 | `filepath.Join(workspaceRoot, userID, workspaceID)` 路径构建在 11 处重复，违反 AGENTS.md 规则6。 |
| 关键位置 | `gateway/handler/workspace_path.go:38`<br>`domain/workspace/service/db_service.go:190`<br>`domain/feishu/service/dispatcher.go:271`<br>`domain/productdoc/service/materialize_service.go:81`<br>`domain/productspace/service/db_service.go:259,373,390`<br>`domain/repository/service/db_service.go:304,314,393,633` |
| 额外问题 | `orchestrator/orchestrator.go:122` 用 `fmt.Sprintf("%s/%s/%s")` 而非 `filepath.Join`，跨平台不兼容。 |
| 修复建议 | 提取为共享工具函数 `ResolveWorkspaceRoot(workspaceRoot, userID, workspaceID string) string`。 |

### 2.4 WebSocket 无心跳/读超时

| 项目 | 说明 |
|------|------|
| 严重程度 | **[中等]** |
| 描述 | 全代码库仅 1 处 `SetWriteDeadline`，**无任何 `SetReadDeadline`、`SetPongHandler`、`PingMessage`**。半开连接将永久阻塞。 |
| 关键位置 | `gateway/handler/gatewayd_proxy.go:117-192` - `serveWS()` 双向代理无读超时、无 ping/pong<br>`domain/personalassistant/websocket.go:64` - `conn.ReadMessage()` 无读超时，仅 write 有 10s 超时 |
| 修复建议 | 添加 ping/pong 心跳机制和 `SetReadDeadline`。 |

### 2.5 `GatewaydClient.run()` 的 `c.done` 从不关闭，goroutine 无法停机

| 项目 | 说明 |
|------|------|
| 文件 | `agent/client/http.go:73` |
| 严重程度 | **[中等]** |
| 描述 | `c.done` 全代码库无 `close()` 调用，`GatewaydClient` 无 `Close()`/`Shutdown()` 方法，goroutine 永远无法优雅停止。 |
| 修复建议 | 添加 `Close()` 方法关闭 `c.done` channel。 |

### 2.6 productspace 版本回滚中 15 处 `_ =` 忽略文件操作错误

| 项目 | 说明 |
|------|------|
| 文件 | `domain/productspace/service/db_service.go:1273-1479` 等 |
| 严重程度 | **[中等]** |
| 描述 | `stubDeleteFile`/`stubWriteFile` 错误被静默忽略，可能导致文件系统状态不一致且无日志。 |
| 修复建议 | 至少记录日志，关键操作失败应中止回滚流程。 |

### 2.7 `fmt.Printf` 用于生产代码

| 项目 | 说明 |
|------|------|
| 文件 | `domain/agentconfig/service/db_service.go:50,285` |
| 严重程度 | **[中等]** |
| 描述 | 生产代码用 `fmt.Printf` 而非 `log.Printf`，输出到 stdout 而非日志流。 |
| 修复建议 | 替换为 `log.Printf`。 |

### 2.8 AgentType JSON 构建用 `fmt.Sprintf` 而非 `json.Marshal`

| 项目 | 说明 |
|------|------|
| 文件 | `domain/agentruntime/service/service.go:279` |
| 严重程度 | **[中等]** |
| 描述 | `fmt.Sprintf('[{"type":"%s"}]', filter.AgentType)` 构建 JSON 字符串，若 `filter.AgentType` 包含双引号或反斜杠会破坏 JSON 结构。 |
| 修复建议 | 使用 `json.Marshal` 正确转义。 |

### 2.9 10 个超长函数（100-316 行）

| 文件:行号 | 函数名 | 行数 | 圈复杂度 | 严重程度 |
|---|---|---|---|---|
| `gateway/handler/agui.go:192` | `AgentRun()` | 766 | 157 | [严重]（见 1.7） |
| `gateway/server/server.go:70` | `New()` | 316 | 12 | [中等] |
| `gateway/handler/session.go:106` | `CreateSession()` | 171 | 33 | [中等] |
| `agent/client/agui_client.go:172` | `AGUIClient.Run()` | 165 | 27 | [中等] |
| `domain/productspace/service/db_service.go:2066` | `ImportPrototype()` | 156 | 42 | [中等] |
| `config/config.go:195` | `Load()` | 143 | 12 | [轻微] |
| `domain/productspace/service/db_service.go:1746` | `RestoreVersion()` | 135 | 26 | [中等] |
| `domain/productspace/service/db_service.go:898` | `syncPrototypeFilesFromDisk()` | 130 | 33 | [中等] |
| `domain/repository/service/db_service.go:731` | `GetDetails()` | 119 | 30 | [中等] |
| `domain/productdoc/service/version_service.go:38` | `ListWorkspaceVersions()` | 112 | 23 | [中等] |

### 2.10 domain 模块间直接依赖，耦合严重

| 来源 domain | 目标 domain | 文件 | 严重程度 |
|---|---|---|---|
| `productspace` | `productdoc` | `service/db_service.go:19`, `service/service.go:9` | [中等] |
| `productspace` | `workitem` | `handler.go:16-17` | [中等] |
| `productspace` | `workspace` | `service/db_service.go:21` | [中等] |
| `team` | `identity` | `handler.go:8` | [轻微] |
| `workspace` | `identity` | `handler.go:11` | [轻微] |
| `agentruntime` | `identity` | `handler.go:11` | [轻微] |

**说明**：`domain/productspace` 是依赖最重的模块，直接 import 了 3 个其他 domain 的 service/object。DDD 原则下 domain 间应通过领域事件或共享接口解耦。多数模块对 `identity` 的依赖是 handler 层为获取用户信息，建议提取为通用 `UserInfoProvider` 接口注入。

### 2.11 错误码魔法值硬编码

| 项目 | 说明 |
|------|------|
| 严重程度 | **[中等]** |
| 描述 | 100+ 处 `WriteJSONError(w, status, code, msg)` 调用中错误码 `1`（通用）、`2`（未授权）、`3`（权限不足）以魔法数字出现。仅 `domain/repository/handler.go:14` 定义了 `const defaultErrorCode = 1`，其余模块全部硬编码。 |
| 修复建议 | 提取为共享常量 `ErrCodeGeneral = 1`、`ErrCodeUnauthorized = 2`、`ErrCodeForbidden = 3`。 |

### 2.12 飞书 token 管理逻辑重复 3 处

| 项目 | 说明 |
|------|------|
| 严重程度 | **[中等]** |
| 描述 | `FeishuReplier`、`FeishuCardKitManager`、`GroupHistoryFetcher` 三个结构体各自实现了几乎相同的 `ensureToken` 逻辑（获取/刷新 tenant_access_token，提前 5 分钟刷新）。 |
| 修复建议 | 提取为共享的 `FeishuTokenManager` 组合复用。 |

### 2.13 SQL LIMIT 硬编码

| 文件 | 行号 | 值 | 严重程度 |
|------|------|-----|----------|
| `domain/notification/service/db_service.go` | 75 | `LIMIT 50` | [中等] |
| `domain/notification/service/db_service.go` | 128 | `LIMIT 10` | [中等] |
| `domain/team/service/service.go` | 1146,1154,1236 | `LIMIT 10` | [中等] |
| `domain/agentruntime/service/service.go` | 214 | `pageSize = 10` | [中等] |

### 2.14 productspace 错误包装丢失错误链

| 项目 | 说明 |
|------|------|
| 文件 | `domain/productspace/service/db_service.go:94,159,175` |
| 严重程度 | **[中等]** |
| 描述 | `fmt.Errorf("%w: %s", ErrInvalidInput, err.Error())` 用 `%s` 格式化内部错误，丢失错误链；`fmt.Errorf("%w: %v", ErrForbidden, err)` 用 `%v` 无法 `errors.Is`/`errors.As` 追溯。 |
| 修复建议 | 统一使用 `%w` 包装错误。 |

### 2.15 handler 层通过全局变量注入 service 实例

| 项目 | 说明 |
|------|------|
| 严重程度 | **[中等]** |
| 描述 | 几乎所有 domain handler 通过包级 `var defaultXxxService` 变量 + `Init()` 函数注入 service，难以测试且隐式依赖不清晰。 |
| 关键位置 | `domain/identity/handler.go:13`、`domain/notification/handler.go:13`、`domain/workitem/handler.go:13` 等 |
| 修复建议 | 改为通过构造函数注入或使用接口字段。 |

---

## 三、轻微问题（P3）

### 3.1 死代码

| 文件 | 行号 | 说明 |
|------|------|------|
| `domain/prototypetemplate/service/service.go` | 376-384 | `resolvePkgManager()` 函数从未被调用（Install 已改用 stubclient.NpmInstall），`"os/exec"` import 为死代码 |
| `gateway/stubclient/client.go` | 59 | `DefaultOrPanic()` 从未被调用，存在误用导致 panic 风险 |

### 3.2 146 条路由路径以魔法字符串内联

| 文件 | 行号 | 严重程度 |
|------|------|----------|
| `gateway/server/server.go` | 151-385 | [轻微] |

**说明**：路由路径以字符串字面量散落在 `New()` 中，未集中定义为常量。

### 3.3 `var _ = pq.FormatTimestamp` hack 式导入保留

| 文件 | 行号 | 严重程度 |
|------|------|----------|
| `domain/agentruntime/service/service.go` | 334 | [轻微] |

**说明**：用 `var _ = pq.FormatTimestamp` 抑制 unused import 警告，违反 AGENTS.md 规则8。应使用空白导入 `_ "github.com/lib/pq"` 或移除。

### 3.4 `reaper()` goroutine 无退出机制

| 文件 | 行号 | 严重程度 |
|------|------|----------|
| `agent/chat/session/session.go` | 35,40-54 | [轻微] |

**说明**：`reaper()` 为 `for { <-ticker.C; ... }` 无限循环，无 `<-done`/context 退出机制。`NewSessionStore()` 无返回 stop 函数。

### 3.5 `StartDocAdoptionCleanupTask` 返回的 stop 函数被丢弃

| 文件 | 行号 | 严重程度 |
|------|------|----------|
| `gateway/server/server.go` | 143 | [轻微] |

**说明**：传入 `context.Background()` 永不取消，返回的 stop 函数被 `_` 丢弃，清理任务 goroutine 无法被停止。

### 3.6 WebSocket `CheckOrigin` 始终返回 `true`

| 文件 | 行号 | 严重程度 |
|------|------|----------|
| `gateway/handler/gatewayd_proxy.go` | 18-20 | [轻微] |
| `domain/personalassistant/websocket.go` | 13-15 | [轻微] |

**说明**：存在 CSWSH（跨站 WebSocket 劫持）风险。

### 3.7 无结构化日志/日志级别

| 严重程度 | [轻微] |
|------|------|
| 描述 | 全代码库仅使用标准库 `log.Printf`/`log.Println`，无结构化日志、无日志级别。无法区分 Error/Warn/Info/Debug。 |
| 修复建议 | 引入 `log/slog`（Go 1.22+）支持结构化分级日志。 |

### 3.8 内联超时魔法值

| 文件 | 行号 | 值 | 严重程度 |
|------|------|-----|----------|
| `agent/client/agui_client.go` | 220,239,310 | `2*time.Minute` | [轻微] |
| `agent/client/agui_client.go` | 433 | `5*time.Second` | [轻微] |
| `agent/client/http.go` | 53 | `30*time.Second` | [轻微] |
| `domain/agentconfig/handler.go` | 40 | `30*time.Second` | [轻微] |
| `domain/feishu/service/group_history.go` | 228 | `1e12`（毫秒时间戳判断阈值） | [轻微] |
| `domain/feishu/service/group_history.go` | 237-241 | `50`（飞书 API 单页上限） | [轻微] |

### 3.9 `repository` 服务硬编码 `"projects"` 字符串

| 文件 | 行号 | 严重程度 |
|------|------|----------|
| `domain/repository/service/db_service.go` | 304,314,393 | [轻微] |

**说明**：`filepath.Join(s.workspaceRoot, userID, workspaceID, "projects", safeName)` 中 `"projects"` 硬编码，而其他地方使用 `repository.DirDevJobs` 常量。

### 3.10 gitExec 函数重复定义

| 文件 | 行号 | 说明 |
|------|------|------|
| `orchestrator/helpers.go` | 55-65 | `gitExec` 返回 string，忽略 error |
| `domain/repository/service/db_service.go` | 853-859 | `gitExec` 返回 (string, error) |
| `gateway/handler/stats.go` | 308-318 | `execGitLogDates` 独立实现 |

**说明**：三处各自实现了通过 stubclient 执行 git 命令的 wrapper，签名和错误处理各不相同。

---

## 四、良好实践（已确认合规）

以下方面做得规范，无需修改：

1. **SQL 注入防护** - 所有用户输入均通过参数化查询（`$1, $2, ...` 占位符），`fmt.Sprintf` 拼接 SQL 仅用于常量标识符。
2. **LIKE 通配符转义** - `escapeLikePattern` 函数正确转义 `%`、`_`、`\`。
3. **productspace 路径安全防护** - `resolveProductSpacePathWithBase` 实现了完整的路径安全防护链（validateID + rejectSymlink + Abs + HasPrefix）。
4. **extractZip 路径穿越防护** - `extractZipEntry` 跳过符号链接、校验 `filepath.Rel` 不逃逸。
5. **SQL rows.Close()** - 全部 ~70 处 `db.Query` 均正确使用 `defer rows.Close()`。
6. **HTTP response body** - 所有 `client.Do(req)` 均有 `defer resp.Body.Close()`。
7. **配置集中管理** - `config/config.go` 集中管理，环境变量读取未散落。
8. **敏感信息日志** - 未发现密码/token/SSH key 明文被日志记录。
9. **materialize_service 路径校验** - `resolveMaterializeTarget` 使用 `filepath.Clean` + `HasPrefix` 校验。
10. **SSE buffer 机制** - 内存/Redis 两种实现，支持崩溃恢复与重放。

---

## 五、优先修复计划

| 优先级 | 修复项 | 影响范围 | 预估工作量 |
|--------|--------|---------|-----------|
| **P0** | SSH key 加密存储 + API 响应过滤字段 | 安全漏洞 | 中 |
| **P0** | 添加通用 `safeGo()` 包装 `defer recover()` | 18处 goroutine | 小 |
| **P0** | `requirementsSummaryCache` 加 `sync.RWMutex` | 数据竞争 | 小 |
| **P0** | `code_write_node` session/message 错误处理 | 数据一致性 | 小 |
| **P0** | `resolveWorkspacePath` 添加 `validateID` | 路径遍历 | 小 |
| **P1** | 飞书 token 管理提取为共享 `FeishuTokenManager` | 3处数据竞争 | 中 |
| **P1** | `AgentRun()` 拆分为子函数 | 可维护性 | 大 |
| **P1** | `context.Background()` -> 请求 context | 可取消性 | 中 |
| **P1** | WebSocket 心跳机制 | 连接可靠性 | 中 |
| **P1** | `GatewaydClient.Close()` 方法 | 优雅停机 | 小 |
| **P2** | 胖接口拆分（TeamService 等） | 架构清晰度 | 大 |
| **P2** | 路径构建提取为共享函数 | 消除重复 | 小 |
| **P2** | 错误码常量化 | 代码规范 | 小 |
| **P2** | 字符串匹配错误 -> 类型化错误 | 健壮性 | 中 |
| **P2** | `fmt.Printf` -> `log.Printf` | 日志规范 | 小 |
| **P3** | 死代码清理 | 代码整洁 | 小 |
| **P3** | 路由路径常量化 | 代码规范 | 小 |
| **P3** | 结构化日志 | 可观测性 | 大 |

---

# 附录：SOLID 原则与模块划分专项 Review

> 审查范围：126 个 .go 文件（排除 _test.go），共 33,199 行
> 重点：模块划分合理性、文件大小与职责单一性、SOLID 原则遵循情况

---

## 附录一：模块/包划分不合理

### A1.1 [严重] `domain/team/` 混装 Skill 和 Prompt 两个不相关领域

- **文件**：`domain/team/service/service.go`（1254 行）、`domain/team/handler.go`（590 行）
- **问题**：`TeamService` 接口含 22 个方法，强行把 Skill 市场（11 方法）和 Prompt 市场（11 方法）塞在一起。Skill handler 只用 Skill 方法，Prompt handler 只用 Prompt 方法，二者无任何共享逻辑。包名 `team` 无法准确描述职责。
- **修复建议**：拆分为 `domain/skill/` 和 `domain/prompt/`，各自独立定义接口、object、handler。

### A1.2 [严重] `domain/identity/` 混装用户管理与租户管理

- **文件**：`domain/identity/service/service.go`（393 行）、`domain/identity/handler.go`（381 行）
- **问题**：`UserService` 接口 14 方法混合用户管理（6 方法）和租户管理（8 方法），handler 中 `Users/Me/Login` 与 `Tenants/TenantMembers` 是两组互不相关的处理器。
- **修复建议**：拆分为 `domain/user/` 和 `domain/tenant/`。

### A1.3 [严重] `domain/productspace/` 职责严重过载（7 个子领域）

- **文件**：`domain/productspace/service/db_service.go`（3228 行）
- **问题**：`ProductSpaceService` 接口 29 方法，承载 7 个子领域：条目 CRUD、版本管理、文件系统操作、原型批注、原型分享、需求级统一分享、文档采纳+清理任务。且与 `domain/productdoc/` 职责重叠（都管"产品文档"和"文档分享批注"）。
- **修复建议**：将"需求级统一分享"独立为 `domain/share/`；将"文档采纳"移入 `productdoc`；文件工具函数提取到 `internal/fileutil` 包。

### A1.4 [严重] `domain/workspace/` 混装 5 个不相关子领域

- **文件**：`domain/workspace/service/db_service.go`（1072 行）
- **问题**：`WorkspaceService` 接口 24 方法混合：Workspace CRUD、成员管理（7方法）、Agent 配置、规范管理、CICD 配置。`EnsureUserWorkspaceDirs` 做文件系统目录创建，属于基础设施操作。
- **修复建议**：拆为 `domain/workspace/`（仅 Workspace+成员）、Agent 方法并入 `domain/agentconfig/`、规范+CICD 独立为 `domain/standard/`。

### A1.5 [中等] `domain/repository/` 混装仓库 CRUD + Git 操作 + 语言检测 + 统计

- **文件**：`domain/repository/service/db_service.go`（1715 行）
- **问题**：18 方法混合 6 类职责：仓库配置 CRUD、用户级仓库同步、同步锁文件管理、Git 文件操作、仓库扫描与统计、语言检测/统计（含硬编码 `languageColorMap`）。
- **修复建议**：拆出 `internal/gitutil`（git 命令封装）、`internal/langdetect`（语言检测+颜色映射+行数统计）。

### A1.6 [轻微] `agent/orchestrator/` 与顶层 `orchestrator/` 包名冲突

- **问题**：两者均为 `package orchestrator`，但职责完全不同（agent 会话列表查询 vs 需求开发评审流程编排），极易混淆。
- **修复建议**：将 `agent/orchestrator/` 重命名为 `agent/sessionmanager/` 或合并入 `agent/chat/`。

---

## 附录二：文件过大排名与拆分方案

### 超过 500 行的文件清单

| 行数 | 文件 | 严重程度 |
|-----:|------|----------|
| **3228** | `domain/productspace/service/db_service.go` | [严重] |
| **1715** | `domain/repository/service/db_service.go` | [严重] |
| **1330** | `gateway/handler/agui.go` | [严重] |
| **1254** | `domain/team/service/service.go` | [严重] |
| **1082** | `domain/productspace/handler.go` | [中等] |
| **1072** | `domain/workspace/service/db_service.go` | [严重] |
| 704 | `domain/workspace/service/prompt_service.go` | [中等] |
| 702 | `gateway/server/server.go` | [中等] |
| 686 | `domain/workspace/handler.go` | [中等] |
| 675 | `domain/productdoc/handler.go` | [轻微] |
| 633 | `agent/client/agui_client.go` | [轻微] |
| 630 | `orchestrator/req_dev_review_flow.go` | [中等] |
| 590 | `domain/team/handler.go` | [轻微] |
| 571 | `agent/client/http.go` | [轻微] |
| 550 | `domain/agentconfig/service/db_service.go` | [轻微] |

### A2.1 `productspace/service/db_service.go`（3228 行）拆分方案

| 新文件 | 职责 | 行数估算 |
|--------|------|---------|
| `db_service.go` | struct + 构造 + 权限校验 | ~200 |
| `fileutil.go` | 路径/文件/sanitize/mime 工具 | ~600 |
| `item_service.go` | Item CRUD + Tree | ~700 |
| `version_service.go` | 版本管理 | ~500 |
| `folder_service.go` | 文件夹 + 导入 | ~400 |
| `comment_service.go` | 批注 | ~120 |
| `prototype_share.go` | 原型分享 | ~250 |
| `requirement_share.go` | 需求分享 | ~700 |
| `doc_adoption_cleanup.go` | 清理任务 | ~80 |

### A2.2 `repository/service/db_service.go`（1715 行）拆分方案

| 新文件 | 职责 | 行数估算 |
|--------|------|---------|
| `db_service.go` | struct + 仓库 CRUD | ~400 |
| `sync_lock.go` | 同步锁/错误/进度文件管理 | ~300 |
| `user_repo.go` | 用户级仓库同步 | ~120 |
| `scanner.go` | 仓库扫描 + 语言检测/统计 | ~400 |
| `file_tree.go` | 文件树 + 文件内容 + gitignore | ~350 |
| `branch_service.go` | 分支 + Git 操作 | ~300 |

### A2.3 `gateway/handler/agui.go`（1330 行）- `AgentRun` 768 行拆分方案

按阶段提取私有方法，主函数收敛为线性编排（< 80 行）：
- `parseRunRequest()` - 请求解析与路径解析
- `resolveOrCreateSession()` - session 复用决策
- `prepareSSEStream()` - SSE 握手
- `applySlashCommands()` - 指令拦截
- `streamAgentEvents()` - 事件流转发
- `finalizeRun()` - 落库与标题生成

文件拆分：`agui.go` / `agui_run.go` / `agui_session.go` / `agui_prompt.go` / `agui_sse.go` / `agui_helpers.go`

### A2.4 `team/service/service.go`（1254 行）拆分方案

- 建立 `domain/team/object/types.go`（18 个 struct + 常量）
- `service.go` 仅留接口
- `db_service.go` Skill 实现 / `prompt_service.go` Prompt 实现 / `category_service.go` 分类 / `stats_service.go` 统计

### A2.5 `workspace/service/db_service.go`（1072 行）拆分方案

| 新文件 | 职责 |
|--------|------|
| `db_service.go` | struct + Workspace CRUD |
| `member_service.go` | 成员管理 |
| `agent_service.go` | Agent 配置 |
| `standard_service.go` | 规范管理 |
| `cicd_service.go` | CICD 配置 |

---

## 附录三：SRP（单一职责）违反

### A3.1 [严重] `DBProductSpaceService` 承担 7 个不相关职责
见 A1.3。`StartDocAdoptionCleanupTask` 甚至启动后台 goroutine，属于基础设施调度职责。

### A3.2 [严重] `DBWorkspaceService` 承担 5 个不相关职责
见 A1.4。`EnsureUserWorkspaceDirs` 做文件系统操作，不属于领域服务。

### A3.3 [严重] `AGUIHandler.AgentRun` 单函数承担十余项职责
见 A2.3。

### A3.4 [中等] handler 层泄漏业务编排逻辑
- `productspace/handler.go` ImportPrototype/ImportDoc 跨服务编排 `workItemSvc.CreateDocLink` + `CreateDesignVersion`
- `workspace/handler.go` Members POST 做邮箱->userID 解析
- `workspace/handler.go` Mine 循环调用 `EnsureUserWorkspaceDirs`

### A3.5 [中等] `DBFeishuService` 同时做持久化+agent调度+消息分发
`domain/feishu/service/dispatcher.go` 混合用户绑定查询、agent 运行调度、会话持久化、消息消费。

---

## 附录四：OCP（开闭原则）违反

### A4.1 [中等] 飞书意图识别硬编码前缀列表 + switch 分发
`domain/feishu/service/intent.go` 新增意图需同时修改 5 处。应改为注册表模式。

### A4.2 [中等] 斜杠指令多处硬编码
`gateway/handler/agui.go` 新增指令需修改至少 4 处（LONG_TASK_COMMANDS、反馈文案、fallback 判断、command_config_defaults.go）。应统一为指令注册表。

---

## 附录五：ISP（接口隔离）违反

### 接口方法数统计

| 接口 | 方法数 | 严重程度 |
|------|--------|----------|
| `ProductSpaceService` | **29** | [严重] |
| `WorkspaceService` | **24** | [严重] |
| `TeamService` | **22** | [严重] |
| `RepositoryService` | 18 | [中等] |
| `WorkItemService` | 14 | [中等] |
| `ProductDocService` | ~15 | [轻微] |
| `AgentConfigService` | 9 | 良好 |
| `FeishuService` | 5 | 良好 |
| `NotificationService` | 8 | 良好 |

### A5.1 [严重] `TeamService` 强迫 Skill handler 依赖 Prompt 方法（反之亦然）
### A5.2 [严重] `ProductSpaceService` 29 方法，调用方仅用子集
### A5.3 [中等] `RepositoryService` 让配置管理调用方依赖 git 文件操作方法

---

## 附录六：DIP（依赖倒置）违反

### A6.1 [严重] 17 个 domain handler 通过包级全局变量 + Init() 注入依赖

所有 domain handler 使用 `var defaultXxxService` + `func Init()` 模式，而非构造函数注入。唯独 `productspace/handler.go` 使用了正确的 `Handler struct` + `NewHandler` 模式。

**修复建议**：统一改为构造函数注入，参照 productspace 模式。

### A6.2 [严重] notification/workitem 通过全局回调变量耦合编排层

`var onAction ActionCallback` + `SetActionCallback` 用全局函数指针反向回调，handler 与 orchestrator 隐式双向耦合。

### A6.3 [中等] productspace service 跨域依赖 productdoc 的 object 类型

`import productdocobject` 直接返回 `productdocobject.ShareComment`，形成跨域耦合。

### A6.4 [中等] `gateway/server/server.go`（702 行）通过全局变量分发

组装根构造具体实现后通过 `domain.Init(svc)` 注入全局变量，而非直接挂到路由。

---

## 附录七：文件组织问题

### A7.1 [严重] 4 个 domain 缺 `object/` 目录

| domain | 类型定义位置 | 问题 |
|--------|------------|------|
| `agentconfig` | `service/service.go` | 配置 DTO 应在 object/ |
| `repository` | `service/service.go` | ScannedRepository/RepositoryDetails/FileNode 等应在 object/ |
| `team` | `service/service.go` | 18 个 struct 全在 service.go |
| `workspace` | `service/service.go` | AgentPolicy/StandardRequest/CICDRequest 等应在 object/ |

### A7.2 [中等] `productspace/handler.go` 内嵌 140 行 JS/CSS 字符串

`prototypeAnnotationScript`（line 44-176）是前端代码硬编码在 Go handler 中。应移至静态文件用 `embed` 引入。

### A7.3 [中等] `workitem/platforms.go` 位置不当

定义 `PlatformInfo` 类型和 `knownPlatforms` 常量，应移入 `object/platforms.go`。

### A7.4 [中等] `team/service/service.go` 类型+接口+实现三合一

其他 domain 均遵循 object/(类型) + service.go(接口) + db_service.go(实现) 分离约定，team 包全部塞在一个文件。

---

## 附录八：SOLID 专项修复优先级

| 优先级 | 修复项 | 影响范围 | 预估工作量 |
|:---:|---|---|---|
| **P0** | `productspace/service/db_service.go`（3228行）拆为 9 个文件 | 单文件 < 700 行 | 大 |
| **P0** | `agui.go` 的 `AgentRun`（768行）拆方法再拆文件 | 单方法 < 100 行 | 大 |
| **P0** | `team` 拆为 `skill` + `prompt` 包，补建 object/ | 消除 ISP 违反 | 大 |
| **P1** | `repository/service/db_service.go`（1715行）按职责拆 6 文件 | 单文件 < 450 行 | 中 |
| **P1** | `productspace/handler.go`（1082行）按子域拆 6 文件 | 单文件 < 350 行 | 中 |
| **P1** | `workspace/service/db_service.go`（1072行）按子能力拆 5 文件 | 单文件 < 450 行 | 中 |
| **P1** | 17 个 handler 全局变量 -> 构造函数注入（参照 productspace） | DIP 合规 | 大 |
| **P2** | `identity` 拆为 `user` + `tenant` | 模块清晰 | 中 |
| **P2** | `agent/orchestrator/` 重命名消除包名冲突 | 包名唯一 | 小 |
| **P2** | 4 个 domain 补建 `object/` 目录 | 类型/接口/实现分离 | 小 |
| **P2** | `orchestrator/req_dev_review_flow.go`（630行）按 Node 拆分 | 单文件 < 200 行 | 中 |
| **P3** | handler 层业务编排逻辑提取为独立编排服务 | SRP 合规 | 大 |
| **P3** | 飞书意图识别 + 斜杠指令改为注册表模式 | OCP 合规 | 中 |
| **P3** | `productspace/handler.go` JS/CSS 移出为 embed 文件 | 文件组织 | 小 |
| **P3** | `workitem/platforms.go` 移至 `object/` | 类型归位 | 小 |
